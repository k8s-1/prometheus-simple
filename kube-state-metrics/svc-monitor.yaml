apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  labels:
    app: kube-state-metrics
  name: kube-state-metrics
  namespace: default
spec:
  selector:
    matchLabels:
      app: kube-state-metrics
  endpoints:
  - port: http-main
  - port: http-self
